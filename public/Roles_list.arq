#QueryScope=Roles
#QueryReturnType=Table
#QueryResults=The liist of BDRC Roles
#QueryParams=L_LANG
#QueryUrl=/Roles_list?L_LANG=en

#param.L_LANG.type=string
#param.L_LANG.isLucene=false
#param.L_LANG.desc=lang tag filter for the results
#param.L_LANG.example=en

#output.?URI.type=URI
#output.?URI.desc=the resource ID of the place
#output.?names.type=string
#output.?names.desc=names of the roles, filter by lang tag

select ?URI (group_concat(?name) as ?names)
where {
  ?URI a :Role;
     skos:prefLabel ?name .
  FILTER (lang(?name) = "?L_LANG")
} group by ?URI 
